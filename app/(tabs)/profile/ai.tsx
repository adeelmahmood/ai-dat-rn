import { View, Text } from "react-native";
import React from "react";
import { Stack, Tabs } from "expo-router";

const ai = () => {
    return (
        <View>
            <Text>ai</Text>
        </View>
    );
};

export default ai;
