import { View, Text } from "react-native";
import React from "react";

const Dates = () => {
    return (
        <View>
            <Text>Dates</Text>
        </View>
    );
};

export default Dates;
